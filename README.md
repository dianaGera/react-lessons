#### Learning React with [Kent C. Dodds](https://github.com/kentcdodds)

## Set up
bash ``
# run the server
npx browser-sync start --server --files "templates/*.html" "static/css/*.css" --no-open --no-notify --directory
``